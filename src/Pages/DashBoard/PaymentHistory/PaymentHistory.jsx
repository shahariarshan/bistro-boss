import SectionTitle from "../../../SectionTitle";
// import useAuth from "../../../hooks/useAuth";


const PaymentHistory = () => {
    // const {user} =useAuth()
    return (
        <div>
            <SectionTitle subHeading='Details' heading='PAyment History'></SectionTitle>
        </div>
    );
};

export default PaymentHistory;